package de.focusshift.zeiterfassung.timeentry;

import org.springframework.stereotype.Controller;

/**
 * Marker interface that can be used by View-{@linkplain Controller}s to highlight
 * "I render the time-entry-form fragment and need its {@linkplain de.focusshift.zeiterfassung.settings.TimeEntrySettings} view model attribute"
 */
public interface HasTimeEntryForm {
}
